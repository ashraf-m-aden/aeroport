<template>
  <div>
    <div class="content">
      Utilisation simultanée des transitions et animations Vue a besoin
      d’attacher des écouteurs d’évènement pour savoir quand une transition est
      terminée. Cela peut être transitionend ou animationend, selon le type de
      règles CSS appliquées. Si vous utilisez seulement l’une ou l’autre, Vue
      peut automatiquement déterminer le type correct. Toutefois, dans certains
      cas, vous pouvez les avoir tous les deux sur le même élément, par exemple
      avoir une animation CSS déclenchée par Vue, ainsi qu’un effet de
      transition CSS lors du survol. Dans ces cas, vous devez explicitement
      déclarer le type dont vous voulez que Vue se soucie dans un attribut type,
      avec une valeur à animation ou transition.
    </div>
        <div class="content">
      Utilisation simultanée des transitions et animations Vue a besoin
      d’attacher des écouteurs d’évènement pour savoir quand une transition est
      terminée. Cela peut être transitionend ou animationend, selon le type de
      règles CSS appliquées. Si vous utilisez seulement l’une ou l’autre, Vue
      peut automatiquement déterminer le type correct. Toutefois, dans certains
      cas, vous pouvez les avoir tous les deux sur le même élément, par exemple
      avoir une animation CSS déclenchée par Vue, ainsi qu’un effet de
      transition CSS lors du survol. Dans ces cas, vous devez explicitement
      déclarer le type dont vous voulez que Vue se soucie dans un attribut type,
      avec une valeur à animation ou transition.
    </div>
        <div class="content">
      Utilisation simultanée des transitions et animations Vue a besoin
      d’attacher des écouteurs d’évènement pour savoir quand une transition est
      terminée. Cela peut être transitionend ou animationend, selon le type de
      règles CSS appliquées. Si vous utilisez seulement l’une ou l’autre, Vue
      peut automatiquement déterminer le type correct. Toutefois, dans certains
      cas, vous pouvez les avoir tous les deux sur le même élément, par exemple
      avoir une animation CSS déclenchée par Vue, ainsi qu’un effet de
      transition CSS lors du survol. Dans ces cas, vous devez explicitement
      déclarer le type dont vous voulez que Vue se soucie dans un attribut type,
      avec une valeur à animation ou transition.
    </div>
        <div class="content">
      Utilisation simultanée des transitions et animations Vue a besoin
      d’attacher des écouteurs d’évènement pour savoir quand une transition est
      terminée. Cela peut être transitionend ou animationend, selon le type de
      règles CSS appliquées. Si vous utilisez seulement l’une ou l’autre, Vue
      peut automatiquement déterminer le type correct. Toutefois, dans certains
      cas, vous pouvez les avoir tous les deux sur le même élément, par exemple
      avoir une animation CSS déclenchée par Vue, ainsi qu’un effet de
      transition CSS lors du survol. Dans ces cas, vous devez explicitement
      déclarer le type dont vous voulez que Vue se soucie dans un attribut type,
      avec une valeur à animation ou transition.
    </div>

  </div>
</template>

<style lang="scss" scoped>
@import "../../assets/sass/main.scss";
.content {
  font-size: 1.5rem;
  margin-bottom: 3rem;
}
</style>
